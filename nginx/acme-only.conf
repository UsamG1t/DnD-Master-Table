# Временный конфиг для получения первого сертификата (server-init.sh):
# отвечает только на ACME-челлендж по :80.
server {
    listen 80 default_server;
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    location / { return 404; }
}
