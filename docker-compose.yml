# Локальная сборка из исходников (ручной запуск, отладка).
name: dnd-master-table
# Для сервера используется docker-compose.prod.yml с образами из GHCR.
services:
  backend:
    build: ./dnd-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: "sqlite:////data/dnd_app.db"
      DND_FILE_CACHE_DIR: "/data/dnd_file_cache"
      DND_COMMUNITY_CACHE_DIR: "/data/community_dnd_file_cache"
      DND_FILE_CACHE_URL_BASE: "https://www.dnd5eapi.co"
    volumes:
      - ./data:/data
    expose:
      - "8000"

  web:
    build: ./dnd-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - certbot-www:/var/www/certbot:ro
      - certbot-conf:/etc/letsencrypt:ro

  certbot:
    image: certbot/certbot
    volumes:
      - certbot-www:/var/www/certbot
      - certbot-conf:/etc/letsencrypt
    entrypoint: >
      /bin/sh -c 'trap exit TERM;
      while :; do certbot renew --webroot -w /var/www/certbot --quiet;
      sleep 12h & wait $${!}; done'

volumes:
  certbot-www:
  certbot-conf:
